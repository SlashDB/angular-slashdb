{
  "name": "angular-slashdb",
  "version": "0.0.1",
  "description": "Angular provider for SlashDB.",
  "main": "",
  "scripts": {
    "postinstall": "typings install",
    "watch": "tsc -w",
    "watch-ex": "tsc --outDir ./example/lib/ -w",
    "build": "tsc & tsc --outDir ./example/lib/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "SlashDB",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "typescript": "^1.8.10",
    "typings": "^1.3.1"
  }
}
