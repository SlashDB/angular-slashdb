{
  "name": "angular-slashdb",
  "version": "0.0.1",
  "description": "Angular provider for SlashDB.",
  "main": "",
  "scripts": {
    "postinstall": "typings install",
    "watch": "tsc -w",
    "build-dist": "echo 'npm gives an error but all files build just fine - npm is weird that way...' && tsc --outDir ./dist/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "SlashDB",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "typescript": "^1.8.10",
    "typings": "^1.3.1"
  }
}
